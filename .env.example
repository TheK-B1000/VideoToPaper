# Copy to `.env` in the project root and set your real values. `.env` is gitignored.
DATABASE_URL=sqlite:///./data/backend.db

# Optional: higher Semantic Scholar API rate limits when running live evidence retrieval
# (`evidence_retrieval.dry_run` false). Public tier works without this key.
# SEMANTIC_SCHOLAR_API_KEY=
